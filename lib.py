class test():
  def test():
    return

def Matricial(x, y):
  for i in range(len(x, 0)):
    for j in range(len(y, 1)):
      z = [len(y, 0), len(x, 1)]
      z[i, j] = 0
      for k in range(len(x, 1)):
        z[i, j] += x[i, k] * y[k, j];

  return z